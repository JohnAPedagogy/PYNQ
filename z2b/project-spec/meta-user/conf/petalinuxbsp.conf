#User Configuration

#OE_TERMINAL = "tmux"

# QEMU compilation is now fixed - no longer skip
# ASSUME_PROVIDED += "qemu-native"  # REMOVED - QEMU now compiles successfully

# Reuse downloads and sstate from existing projects for faster builds
DL_DIR = "/home/its/tools/yocto/petal/pynq/z2ptx/build/downloads"
SSTATE_DIR = "/home/its/tools/yocto/petal/pynq/z2ptx/build/sstate-cache"

# Enable WIC image generation for bootable SD card images
IMAGE_FSTYPES += "wic wic.bmap wic.bz2"
